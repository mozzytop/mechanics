# fastapi/starlette are intentionally left as minimum-version bounds, not
# exact pins. Hugging Face Spaces' Gradio-SDK base image force-installs
# gradio==6.21.0 on top of this file, and gradio requires starlette>=1.0.1
# (a major version with a breaking TemplateResponse API change: it's now
# TemplateResponse(request, name, context) instead of the old
# TemplateResponse(name, {"request": request, ...})). Our route handlers
# in app/routers/*.py already use the new signature, so pin low here and
# you WILL break the app - let pip resolve a current fastapi/starlette pair.
fastapi>=0.115.2
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-dotenv==1.0.1
httpx==0.27.2
python-multipart>=0.0.18
pydantic>=2.11.10,<=2.12.5
